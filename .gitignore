# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.cer
*.csr
*.mobileprovision

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# Local env files
.env*.local

# TypeScript
*.tsbuildinfo

# Generated native folders (regenerated by `expo prebuild` in CI)
/ios
/android

# Source artifact (the original Claude.ai JSX export, not part of the RN app)
workout-app.jsx

# Local secrets (live on the dev machine, NEVER committed)
secret_*.txt
distribution.cer
apple_distribution*
